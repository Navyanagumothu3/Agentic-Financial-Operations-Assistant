# Optional: set OPENAI_API_KEY to enable live LLM reasoning
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL_FAST=gpt-4o-mini
# OPENAI_MODEL_REASONING=gpt-4o

# Without an API key the system uses rule-based agents (zero cost)
